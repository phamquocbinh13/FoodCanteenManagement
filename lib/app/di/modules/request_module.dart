import 'package:get_it/get_it.dart';

/// Request queue module shell. Sprint 9.
abstract final class RequestModule {
  static void register(GetIt sl) {
    // RequestRepository and use cases registered in Sprint 9.
  }
}
